# ML agent — LangGraph control flow + an OpenAI-compatible client (LiteLLM gateway)
# + the Science-stack clients (MLflow registry, Featureform). KServe is called over
# plain HTTP. Langfuse for tracing. Pinned, small.
fastapi==0.115.6
uvicorn==0.34.0
langgraph==0.2.61
openai==1.59.6
mlflow-skinny==2.19.0
httpx==0.28.1
langfuse==2.57.0
