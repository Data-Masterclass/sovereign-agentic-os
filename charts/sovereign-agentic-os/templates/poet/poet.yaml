{{- if .Values.poetAgent.enabled }}
{{- /*
  Poet agent (bespoke Deployment + Service + PVC). A second LangGraph agent
  (compose -> save): asks the model via LiteLLM for a short poem and writes it
  to a file on a persistent volume — one file per run. Traced in Langfuse.
  Pull the poems to your machine with scripts/get-poems.sh.
*/}}
{{- $p := .Values.poetAgent }}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: poet-poems
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "soa.labels" . | nindent 4 }}
    {{- include "soa.selectorLabels" (dict "ctx" . "component" "poet-agent") | nindent 4 }}
spec:
  accessModes: ["ReadWriteOnce"]
  resources:
    requests:
      storage: {{ $p.storageSize | quote }}
  {{- if .Values.global.storageClass }}
  storageClassName: {{ .Values.global.storageClass | quote }}
  {{- end }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: poet-agent
  namespace: {{ .Release.Namespace }}
  annotations:
    {{- include "soa.syncWaveAnno" (dict "ctx" . "tier" "apps") | nindent 4 }}
  labels:
    {{- include "soa.labels" . | nindent 4 }}
    {{- include "soa.selectorLabels" (dict "ctx" . "component" "poet-agent") | nindent 4 }}
spec:
  replicas: 1
  strategy:
    type: Recreate           # single RWO volume
  selector:
    matchLabels:
      {{- include "soa.selectorLabels" (dict "ctx" . "component" "poet-agent") | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "soa.labels" . | nindent 8 }}
        {{- include "soa.selectorLabels" (dict "ctx" . "component" "poet-agent") | nindent 8 }}
    spec:
      {{- include "soa.priorityClassName" (dict "ctx" . "tier" "app") | nindent 6 }}
      {{- with .Values.global.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      securityContext:
        {{- include "soa.podSecurityContext" . | nindent 8 }}
        runAsUser: 1000
        fsGroup: 1000          # so the non-root agent can write the PVC
      containers:
        - name: poet
          image: "{{ $p.image.repository }}:{{ $p.image.tag }}"
          imagePullPolicy: IfNotPresent     # locally-built image, loaded into kind
          securityContext:
            {{- include "soa.containerSecurityContext" . | nindent 12 }}
            readOnlyRootFilesystem: true
          env:
            - name: PORT
              value: "8000"
            - name: HOME
              value: /tmp
            - name: LITELLM_BASE_URL
              value: "http://agentic-os-litellm:4000/v1"
            - name: LITELLM_API_KEY     # scoped virtual key (not the master key)
              valueFrom: { secretKeyRef: { name: agent-litellm-key, key: apiKey } }
            - name: CHAT_MODEL
              value: {{ $p.chatModel | quote }}
            - name: DEFAULT_TOPIC
              value: {{ $p.defaultTopic | quote }}
            - name: POEMS_DIR
              value: /data/poems
            - name: LANGFUSE_HOST
              value: {{ .Values.langfuseInit.host | quote }}
            - name: LANGFUSE_PUBLIC_KEY
              value: {{ .Values.langfuseInit.publicKey | quote }}
            - name: LANGFUSE_SECRET_KEY
              valueFrom:
                secretKeyRef: { name: langfuse-init, key: LANGFUSE_INIT_PROJECT_SECRET_KEY }
          ports:
            - { name: http, containerPort: 8000 }
          # The agent (app.py main()) runs a blocking wait_for_gateway() warmup —
          # up to 60 retries * 3s = 180s of LiteLLM calls — BEFORE it binds the
          # HTTP server, so /health does not answer until warmup finishes (or
          # gives up). When a dependency is not yet ready (model-server still
          # loading the model, or the litellm virtual key not yet registered),
          # warmup keeps failing and the previous probe layout let livenessProbe
          # (initialDelay 30s + 3*20s ~= 90s) kill the pod before the server ever
          # bound -> permanent CrashLoop. A startupProbe gives the slow start a
          # generous budget (5s * 45 = 225s > the 180s warmup ceiling) and holds
          # liveness/readiness off until the server is actually listening; once
          # warmup gives up it binds regardless, so this breaks the loop even if
          # the dependency is still coming up. (Proper fix = bind first / warm up
          # in a background thread, which needs an image rebuild — see report.)
          startupProbe:
            httpGet: { path: /health, port: http }
            initialDelaySeconds: 5
            periodSeconds: 5
            failureThreshold: 45
          readinessProbe:
            httpGet: { path: /health, port: http }
            initialDelaySeconds: 5
            periodSeconds: 5
            failureThreshold: 30
          livenessProbe:
            httpGet: { path: /health, port: http }
            initialDelaySeconds: 30
            periodSeconds: 20
          resources:
            {{- toYaml $p.resources | nindent 12 }}
          volumeMounts:
            - { name: poems, mountPath: /data/poems }
            - { name: tmp, mountPath: /tmp }
      volumes:
        - name: poems
          persistentVolumeClaim:
            claimName: poet-poems
        - name: tmp
          emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: poet-agent
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "soa.labels" . | nindent 4 }}
    {{- include "soa.selectorLabels" (dict "ctx" . "component" "poet-agent") | nindent 4 }}
spec:
  selector:
    {{- include "soa.selectorLabels" (dict "ctx" . "component" "poet-agent") | nindent 4 }}
  ports:
    - { name: http, port: 8000, targetPort: http }
{{- end }}
